import React from 'react';

const Test = ({dataManager}) => {

    const test = () => {
        // const test = dataManager.getAverage(dataManager.getTeamIdsNew()[1],dataManager.getTeamScores(1));

        // const team_race_object = {team_id: 1, 'eng': false, 'por': false};
        // const test = dataManager.getAverage(team_race_object,dataManager.getTeamScores(1));

        // console.log(test);
    
    }
    
    test();

    return ( 
        <p>
             Should be Ham: 
        </p>
    )
}

export default Test;